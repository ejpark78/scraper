version: '3'

tasks:
  config:
    cmds:
      - '{{.COMPOSE}} -f {{.ROOT_DIR}}/infra/autoheal/docker/compose.yml config'
  up:
    cmds:
      - '{{.COMPOSE}} --profile infrastructure up -d autoheal'
  down:
    cmds:
      - '{{.COMPOSE}} stop autoheal'
  logs:
    cmds:
      - '{{.COMPOSE}} logs -f autoheal'
  ps:
    cmds:
      - '{{.COMPOSE}} ps autoheal'
