# 📋 Task: Clean Up Sites Configuration After Viewer Build (Minor)

이 축약형 태스크 문서는 `make viewer-build` 시 빌드 임시 산출물(`sites.json`)의 자동 정비 작업 진행 현황을 관리합니다.

## 할 일 목록
- [x] `apps/viewer/Makefile` 내 `build` 타겟 명령어에 `rm -f` 정비 명령어 추가
- [x] 자동 커밋 스크립트 실행
