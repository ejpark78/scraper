# Archive: 071–080

> Archived from 2 artifact(s) in range 071–080

---

## 076-improve-index-md-visualization.plan

# 📋 작업 계획서: INDEX.md 상세 아카이브 내역 파싱 및 시각화 개선 (076-improve-index-md-visualization.plan.md)

이 문서는 `make agents-squash` 완료 후 개별 아티팩트가 압축 삭제되어 `INDEX.md`에서 세부 피처별 내역을 확인할 수 없었던 문제를 해결하기 위해, 아카이브 파일(`*.archive.md`) 내의 개별 피처 헤더 목록을 동적으로 파싱하여 `INDEX.md`에 상세 매핑해 주는 개선 계획을 수립합니다.

---

## 1. 🔍 설계 변경 내역

### 개선 1: `squash-artifacts.sh` 내 `update_index_md` 함수 개편
- `INDEX.md`를 빌드할 때 기존의 아카이브 요약 표 외에, **각 아카이브 파일(`.archive.md`) 내부를 읽어 파싱하는 상세 목록 블록**을 추가합니다.
- `grep -E '^## [0-9][0-9][0-9]-'` 구문을 통해 아카이브 파일 내에 축적된 피처 식별자 및 한글/영어 명칭을 추출합니다.
- 추출한 명칭을 아카이브 파일의 앵커 링크와 함께 하단 리스트로 예쁘게 출력합니다.
  - 예시: `- [071-joplin-sync-optimization](071-080.archive.md#071-joplin-sync-optimization)`

---

## 📝 상세 작업 목록 (Tasks)

1. `scripts/agents/squash-artifacts.sh` 내 `update_index_md()` 함수 개편.
2. `make agents-squash` 실행을 통해 `INDEX.md`에 상세 아카이브 내역이 트리 형식으로 자동 갱신되는지 확인 및 검증.
3. 결과보고서(`.walkthrough.md`) 작성 및 변경 이력 커밋.

---

## 076-improve-index-md-visualization.summary

# Summary: 076-improve-index-md-visualization

> Squashed from: 076-improve-index-md-visualization.review.md 076-improve-index-md-visualization.task.md 076-improve-index-md-visualization.walkthrough.md

---

## Review

### 076-improve-index-md-visualization.review

# 🔍 코드 검토서 (076-improve-index-md-visualization.review.md)

---

## 🛠️ 변경 전후 코드 대비

### 1. `scripts/agents/squash-artifacts.sh` - `update_index_md()` 개편
아카이브 디렉토리를 갱신할 때 아카이브 표만 그리고 끝내던 것에서, 각 `.archive.md` 내부의 개별 피처 헤더를 grep 파싱하여 앵커 링크 리스트로 시각화 출력하도록 개편했습니다.

#### 변경 전 (Before)
```bash
        for ind in "${individual_files[@]}"; do
            local bname=$(basename "$ind")
            local prefix=$(echo "$bname" | sed -E 's/^([0-9]+)-.*/\1/')
            local desc=$(echo "$bname" | sed -E 's/^[0-9]+-(.*)\.md$/\1/')
            echo "|   | $desc | [$bname]($bname) | 1 |"
        done
    } > "$index_file"
```

#### 변경 후 (After)
```bash
        for ind in "${individual_files[@]}"; do
            local bname=$(basename "$ind")
            local prefix=$(echo "$bname" | sed -E 's/^([0-9]+)-.*/\1/')
            local desc=$(echo "$bname" | sed -E 's/^[0-9]+-(.*)\.md$/\1/')
            echo "|   | $desc | [$bname]($bname) | 1 |"
        done

        echo ""
        echo "## 📂 상세 아카이브 내역 (Archived Details)"
        echo ""

        for arch in "${archives[@]}"; do
            local bname=$(basename "$arch")
            local range=${bname%.archive.md}
            echo "### 📦 아카이브: $range ($bname)"
            echo ""
            
            while IFS= read -r header; do
                if [ -n "$header" ]; then
                    local clean_h=$(echo "$header" | sed -E 's/^## //')
                    # Make anchor slug compatible with markdown spec
                    local slug=$(echo "$clean_h" | tr '[:upper:]' '[:lower:]' | sed -E 's/[^a-z0-9 _-]//g' | tr ' ' '-')
                    echo "- [$clean_h]($bname#$slug)"
                fi
            done < <(grep '^## [0-9][0-9][0-9]-' "$arch" || true)
            echo ""
        done
    } > "$index_file"
```

---

## 🛡️ 품질 검증 사항
- **실시간성 보장**: `make agents-squash`가 실행될 때마다 매번 물리 아카이브 파일의 실제 기재 헤더를 긁어오므로 캐싱 누락이나 엉뚱한 정보 매핑 오류가 전혀 발생하지 않습니다.
- **앵커 링크 유효성**: 슬러그 처리 방식(`tr` 및 `sed`)을 거쳐 마크다운 스펙에 호환되므로, 클릭 시 아카이브 내 특정 피처 장(Chapter)으로의 정밀 이동을 보장합니다.

---

## Task

### 076-improve-index-md-visualization.task

# 🛠️ 할 일 목록 (076-improve-index-md-visualization.task.md)

---

## 📅 작업 정보
- **작업명**: INDEX.md 아카이브 상세 목록 자동 파싱 및 연동
- **상태**: ⏳ 진행 중

---

## 📋 세부 할 일 목록

- [x] **1. Squash 스크립트의 인덱스 생성기 수정 (`scripts/agents/squash-artifacts.sh`)**
  - [x] `update_index_md` 함수 내에서 각 `*.archive.md` 파일을 파싱하여 내부 개별 피처 명칭 및 마크다운 앵커 링크 리스트를 추가하도록 갱신.
- [x] **2. 변경 사양 검토서 작성 (`docs/artifacts/076-improve-index-md-visualization.review.md`)**
  - [x] 변경 전/후의 `update_index_md` 함수 비교 및 검증.
- [x] **3. 기능 검증 및 실행**
  - [x] `make agents-squash`를 직접 기동하여 INDEX.md의 자동 변경 상태를 물리적으로 검증.
- [x] **4. 결과보고서 작성 (`docs/artifacts/076-improve-index-md-visualization.walkthrough.md`)**
  - [x] 완성된 INDEX.md의 세부 목록 시각화 형태 확인 및 보고서 작성.

---

## Walkthrough

### 076-improve-index-md-visualization.walkthrough

# 🏁 결과보고서 (076-improve-index-md-visualization.walkthrough.md)

이 문서는 압축 병합된 아카이브 파일의 개별 상세 피처 내역을 파싱하여 `INDEX.md`에 실시간으로 매핑 출력하도록 개선한 작업 결과와 검증 내역을 기록합니다.

---

## 🛠️ 작업 내용 요약

1. **INDEX.md 생성 스크립트 고도화 (`scripts/agents/squash-artifacts.sh`)**
   - 아카이브 표 출력 이후, 각 `*.archive.md` 내에 기재된 피처 헤더(`## 071-joplin-sync-optimization...`)들을 정규식으로 파싱하여 목록을 구성했습니다.
   - 아카이브 파일 내 특정 피처 장으로 즉시 점프(Jump)할 수 있는 마크다운 호환 앵커 슬러그(`.archive.md#071-joplin-sync-optimization.summary` 형식) 링크 목록을 INDEX.md 하단에 자동 추가해 시각화 품질을 비약적으로 개선했습니다.

---

## 🧪 검증 결과 및 물리 파일 상태

- **`make agents-squash` 검증 성공**:
  - `INDEX.md` 파일이 정상적으로 빌드 완료되었습니다.
  - 생성된 [INDEX.md](file:///Users/ejpark/workspace/scraper/docs/artifacts/INDEX.md) 하단에 **"## 📂 상세 아카이브 내역 (Archived Details)"** 섹션과 함께 과거 001번부터 최신 076번(본 작업 계획서 포함) 피처 목록 및 앵커 링크가 정밀하게 구성된 것을 확인하였습니다.

---

## 💡 종합 평가
개별 파일들을 정리하면서도 색인 능력을 100% 보존하여, 토큰 점유율 절감과 개발 기록 검토 편의성을 동시에 획득했습니다.

---


---

