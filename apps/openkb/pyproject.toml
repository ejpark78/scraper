[project]
name = "openkb-app"
version = "0.1.0"
description = "OpenKB compiling and wiki deployment scripts"
requires-python = ">=3.13"
dependencies = [
    "openkb==0.3.0",
]

[tool.uv]
prerelease = "allow"
