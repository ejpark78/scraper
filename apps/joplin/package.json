{
  "name": "scraper-joplin-tool",
  "version": "1.0.0",
  "description": "Dockerized Joplin CLI Sync and Web Clipper Push Tool",
  "private": true,
  "scripts": {
    "sync": "ts-node src/index.ts sync",
    "push": "ts-node src/index.ts push"
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
