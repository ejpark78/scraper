[project]
name = "ebook"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymupdf>=1.27.2.3",
    "poethepoet>=0.32.2",
]

[tool.poe.tasks]
summary = "python -m src.process --summary"
analyze = "python -m src.process --analyze"
split = "python -m src.process --split"
md = "python -m src.process --md"
html = "python -m src.process --html"
translate = "python -m src.process --translate"
